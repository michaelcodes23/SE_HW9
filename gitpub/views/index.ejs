<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
    <title>Mixologylicious</title>

</head>
<style>
    body {
        margin: auto auto;
        text-align: center;
        background-color: gray;
        color: white;
        }
    .btn{
        background-color: white;
    }
</style>
<body>
        <h1>Names of Drinks</h1>
    <ul>
        <% for(let i=0;i < drink.length;i++){ %>
            <% const option = drink[i].name.substr(0,1).toUpperCase() + drink[i].name.substr(1) %>
            <li class = "btn btn-outline-primary">
                <a href="/drinks/<%=i%>">
                <%=option%>    
                </a>
            </li>
        <%}%>
    </ul>
    <h1>Food Options</h1>
    <ul>
        <% for(let i=0;i < foods.length;i++){ %>
            <% const option = foods[i].name.substr(0,1).toUpperCase() + foods[i].name.substr(1) %>
            <li class = "btn btn-outline-primary">
                <a href="/drinks/food/<%=i%>">
                <%=option%>    
                </a>
            </li>
        <%}%>
    </ul>
</body>
</html>